server.port=${PORT:9090}

spring.datasource.url=jdbc:mysql://shuttle.proxy.rlwy.net:39597/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

spring.datasource.username=root
spring.datasource.password=gESailOecDOvYQTKQJGpFMJrzvKDeMwi

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Email Configuration - Use Port 587 with STARTTLS
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:465}
spring.mail.username=${SPRING_MAIL_USERNAME:smarthabittracker31@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:dvrj vkbu nlei hdzq}

spring.mail.protocol=smtp

# SMTP Properties for Gmail
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Timeout settings
spring.mail.properties.mail.smtp.connectiontimeout=60000
spring.mail.properties.mail.smtp.timeout=60000
spring.mail.properties.mail.smtp.writetimeout=60000

# Additional Gmail SMTP properties
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.socketFactory.fallback=false
spring.mail.properties.mail.smtp.socketFactory.port=465

# Debug mode for email
spring.mail.properties.mail.debug=true

# App Configuration
app.email.from=smarthabittracker31@gmail.com

app.email.sender.name=Smart Habit Tracker

frontend.url=https://smart-habit-tracker-latests.netlify.app


