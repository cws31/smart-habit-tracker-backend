# Production Configuration
server.port=${PORT:8080}

# ✅ PRODUCTION DATABASE CONFIGURATION
spring.datasource.url=${DATABASE_URL:jdbc:mysql://shuttle.proxy.rlwy.net:39597/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:gESailOecDOvYQTKQJGpFMJrzvKDeMwi}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Email Configuration - Production
spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:smarthabittracker31@gmail.com}
spring.mail.password=${SMTP_PASSWORD:dvrj vkbu nlei hdzq}
spring.mail.protocol=smtp

# SMTP Properties for Gmail
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Timeout settings
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# Debug - false in production
spring.mail.properties.mail.debug=false

# App Configuration
app.email.from=${EMAIL_FROM:smarthabittracker31@gmail.com}
app.email.sender.name=${EMAIL_SENDER_NAME:Smart Habit Tracker}
app.base-url=${BASE_URL:http://localhost:5173}